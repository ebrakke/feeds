services:
  feeds:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - FEEDS_DB=/app/data/feeds.db
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    restart: unless-stopped
